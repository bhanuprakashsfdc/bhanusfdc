# Cloudflare Workers Assets handles SPA routing automatically
# No redirects needed - all non-file requests serve index.html

# Example redirects (uncomment to use):
# /old-page    /new-page    301
# /legacy/*    /new/:splat  301
