# Netlify redirects file (Format: from_path to_path status_code)
# SPA routing is handled by Cloudflare's 404.html fallback automatically

# Example redirects:
# /old-page    /new-page    301
# /legacy/*    /new/:splat  301
